using System;
using System.Linq;
using System.Threading.Tasks;

namespace Exercises
{
    class Program
    {
        static async Task Main(string[] args)
        {
            //    string s = "";

            //    for (int i = 0; i < 1000; i++)
            //    {
            //        s += i;
            //    }

            //;

            //string input = Console.ReadLine();

            //double[] nums = input
            //.Split(" ", StringSplitOptions.RemoveEmptyEntries)
            //.Select(int.Parse)
            //.Where(n => n % 2 == 0)
            //.Select(n => Math.Pow(n, 2) + 1)
            //.ToArray();

            ;

            int a = 5;

            int b = 10;

            Console.WriteLine(a + b);
        }
    }
}
